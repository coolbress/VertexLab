# Changelog

The `vertex-lab` package keeps its own release history in:

- [packages/vertex-lab/CHANGELOG.md](https://github.com/coolbress/VertexLab/blob/main/packages/vertex-lab/CHANGELOG.md)

The old root-level `vertex-lab` release history is intentionally retired as part of the package move into `packages/vertex-lab/`.
